# Task 142 文生图浏览器走查清单

> 执行前提：启动本地 dev server，登录后打开一个 Project，并提前准备好可用的 OpenAI 兼容 LLM Provider 与 NovelAI Provider（真实凭据）。

## 1. 工作台入口
> 2026-08-07 状态：已勾选项为本轮走查确认；真实 LLM/NovelAI 端到端出图与 NovelAI 配置实调仍未验证。
> 2026-08-08 追加：上下文预设文件导入、默认折叠、开关状态、获取模型按钮交互已实测；真实 LLM/NovelAI 端到端仍需有效凭据与网络。
> 2026-08-09 复核：真实 ds-flash LLM 已完成正文 L1 → L2 占位符并写回 Project；Node 代理适配已使产品队列真正访问 NovelAI，但上游两次返回 `HTTP 429`，资产列表仍为 0，因此第 4 节保持未勾选。浏览器自动化会话不稳定，本轮 API/文件证据不替代浏览器人工验收。
> 2026-08-09 后续：按每 `15s` 间隔循环重试后，真实产品请求成功生成并保存 `1216x832` 资产，章节已替换为 Markdown 图片；第 4 节仍保持未勾选，因为本次证据来自 API/文件复核而非浏览器人工操作。
- [x] 鉴权关闭时，不登录也可打开文生图工作台并读取配置/Provider 列表。
- [x] 左侧「文生图」入口可见，点击后弹出独立工作台大窗口。
- [x] 左侧导航包含 LLM / NovelAI / 正文生图 / 角色管理 / 历史图片，切换无布局错乱。

## 2. LLM 配置

- [ ] 新建 `openai_compatible` Provider，填写名称、Base URL、API Key、模型后保存。
- [x] 「获取模型」能拉回模型列表，并可直接选择。
- [ ] 新建 Provider 未保存时，「获取模型」也可点击并基于当前 Base URL/API Key 拉取模型或显示明确错误。
- [ ] 流式生成、发送图片、合并 System/User、重试次数、模型参数保存后重新打开仍保留。
- [x] 流式生成、发送图片、Tagthink 回显等开关显示「已开启/已关闭」，状态一眼可辨。
- [ ] 上下文预设：新建/保存/删除条目，triggerMode、triggerWords、andTriggerWords 生效。
- [ ] 上下文预设支持导入/导出全部；可导入 chatu8 `预设名 -> {entries}` JSON 与导出的完整 JSON；全局配置支持导入/导出。
- [x] 上下文预设默认折叠，导入大量条目后页面不拉长；点击展开后可编辑。
- [x] 上下文预设文件导入实测（2026-08-08）：导入后自动选中并保持折叠，删除测试预设后恢复。
- [x] 五个请求类型（正文生图/角色设计/角色展示/角色服装修改/Tag 修改）绑定存在。
- [ ] 测试工具能直调 LLM 并显示回复；`{{正文}}`、`{{roll 1d6}}`、变量占位符在回复链路中不报错。

## 3. NovelAI 配置

- [ ] 新建 NovelAI Provider，保存模型/采样器/噪点表/Guidance/尺寸/Steps/Seed。
- [ ] 生图方案可把画风串、模型参数、质量预设、替换规则和参考图选择保存为多组配方，并切换当前生效方案。
- [ ] 「翻译固定提示词」能用 LLM 把中文字段翻译成英文 tag。
- [ ] 常用 Tag 输入可追加到正面提示词。
- [ ] 配置档案可另存、读取、删除。
- [ ] Vibe Transfer 可启用，参考图文件名写入 `imageId`；可下载 `.naiv4vibe` 文件。
- [ ] AQT/UCP 质量预设选项与福瑞数据集开关可保存。

## 4. 正文生图端到端

- [ ] 正文生图会话选择 LLM/NovelAI Provider 与章节，生成 L1 块预览。
- [ ] 「写入章节」后正文出现 `<text-to-image-prompt>` 占位符。
- [ ] TipTap 占位符卡片显示标题与「生成图片」按钮。
- [ ] 点击「生成图片」后任务入队并消费，正文占位符被替换为标准 Markdown 图片。
- [ ] 生成的图片能在历史图片分页看到，并能打开原图。

## 5. 角色视觉与头像

- [ ] 角色管理读取 `visual.json`，12 字段可编辑并保存。
- [ ] 「生成角色视觉」能调用 LLM 回填并写入 visual.json。
- [ ] 角色照片 prompt 能生成，头像生成后 photos 数组更新。

## 6. 回归检查

- [ ] 以上操作不触发控制台未捕获错误。
- [ ] 刷新页面后全局配置与 Provider 仍在。
- [ ] 无鉴权本地模式不登录也能保存 Provider，且不报外键错误。
- [x] 云端队列相关设置不出现在界面中；队列只在本机处理。
